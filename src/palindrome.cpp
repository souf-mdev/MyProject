/*#include "palindrome.h"
using namespace std;

int palindrome(std::string s)
{
    std::string p = s;
 
    reverse(p.begin(), p.end());
 
    if (s == p) {
        return "Yes";
    }
    else {
        return "No";
    }
}
 
*/
